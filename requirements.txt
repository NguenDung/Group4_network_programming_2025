## No external dependencies required.
## No external dependencies from standard library are required except Pillow for GIF support.

* **Uses Python standard libraries: tkinter, json, base64, shlex** (I mean there is nothing much for a final project...)
* **Pillow>=8.0.0**
